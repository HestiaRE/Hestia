#!/bin/bash
# info: change web domain name
# options: USER DOMAIN NEW_DOMAIN [RESTART]
#
# example: h-change-web-domain-name alice wonderland.com lookinglass.com yes
#
# This function is used for changing the domain name.

#----------------------------------------------------------#
#                Variables & Functions                     #
#----------------------------------------------------------#

# Argument defenition
user=$1
domain=$2
new_domain=$3
restart=$4

# Includes
# shellcheck source=/etc/hestia/hestia.env
source /etc/hestia/hestia.env
# shellcheck source=/usr/local/hestia/include/main.sh
source $HESTIA/include/main.sh
# shellcheck source=/usr/local/hestia/include/domain.sh
source $HESTIA/include/domain.sh
# shellcheck source=/usr/local/hestia/include/ip.sh
source $HESTIA/include/ip.sh
# load config file
source_conf "$HESTIA/conf/hestia.conf"
web_lock_acquire || exit 1

# Additional argument formatting
format_domain
format_domain_idn
# TODO: $domain_idn not used in this script - maybe $domain should be converted to $doman_idn ?

#----------------------------------------------------------#
#                    Verifications                         #
#----------------------------------------------------------#

check_args '3' "$#" 'USER DOMAIN NEW_DOMAIN [RESTART]'
is_format_valid 'user' 'domain' 'restart'
is_domain_format_valid "$new_domain"
is_system_enabled "$WEB_SYSTEM" 'WEB_SYSTEM'
is_object_valid 'user' 'USER' "$user"
is_object_unsuspended 'user' 'USER' "$user"
is_object_valid 'web' 'DOMAIN' "$domain"
is_object_unsuspended 'web' 'DOMAIN' "$domain"
check_new_domain=$(grep -F "DOMAIN='$new_domain'" $CONF_DIR/users/*/web.conf)
if [ -n "$check_new_domain" ]; then
	check_result "$E_EXISTS" "$new_domain already exists"
fi

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

# Fetching domain variables
get_domain_values 'web'

# Changing domain in web.conf - pattern-side dots escaped, or renaming a.b.com also
# rewrites the aXb.com record
sed -i "s/DOMAIN='${domain//./\\.}'/DOMAIN='$new_domain'/" $USER_DATA/web.conf
# Dots escaped here too, for the reason above: renaming a.b.com otherwise rewrote an alias awb.com to
# the new name, and the duplicate filter below then dropped it - the customer lost an alias silently.
new_alias=$(echo "$ALIAS,$domain" \
	| sed -e "s/,/\n/g" \
	| sed -e "s/${domain//./\\.}/$new_domain/" \
	| sed -e "s/^${new_domain//./\\.}$//g" \
	| sed -e "/^$/d" \
	| sed -e ':a;N;$!ba;s/\n/,/g')

# Updating domain alias
if [ -n "$ALIAS" ] && [ "$ALIAS" != "$new_alias" ]; then
	sed -i "s/ALIAS='$ALIAS'/ALIAS='$new_alias'/" $USER_DATA/web.conf
fi

# Moving document root and domain logs
mv "$HOMEDIR/$user/web/$domain" "$HOMEDIR/$user/web/$new_domain/"
cd /var/log/$WEB_SYSTEM/domains
mv "$domain.log" "$new_domain.log"
mv "$domain.error.log" "$new_domain.error.log"
mv "$domain.bytes" "$new_domain.bytes"
rm -f "$HOMEDIR/$user/web/$new_domain/logs/$domain.*"

# The jail is watching the old filename; a rename is invisible to it.
if [ -n "$FIREWALL_EXTENSION" ]; then
	# shellcheck source=/usr/local/hestia/include/fail2ban.sh
	source $HESTIA/include/fail2ban.sh
	fail2ban_watch_domain del "$domain"
	fail2ban_watch_domain add "$new_domain"
fi

# Updating domain certificates
if [ -e "$USER_DATA/ssl/$domain.crt" ]; then
	cd $USER_DATA/ssl
	mv $USER_DATA/ssl/$domain.crt $USER_DATA/ssl/$new_domain.crt
	mv $USER_DATA/ssl/$domain.ca $USER_DATA/ssl/$new_domain.ca
	mv $USER_DATA/ssl/$domain.pem $USER_DATA/ssl/$new_domain.pem
	mv $USER_DATA/ssl/$domain.key $USER_DATA/ssl/$new_domain.key
	rm -f $HOMEDIR/$user/conf/web/$domain/ssl/$domain.*
fi

# Deleting vhost configuration
del_web_config "$WEB_SYSTEM" "$TPL.tpl"

# Deleting proxy
if [ -n "$PROXY_SYSTEM" ]; then
	del_web_config "$PROXY_SYSTEM" "$PROXY.tpl"
	# Zone and cached data are keyed by the old name; the closing rebuild recreates both.
	# Literal nginx path (proxy cache is nginx-only), matching the add/delete commands
	rm -f "/etc/nginx/conf.d/cache-zone.$domain.conf"
	rm -rf "/var/cache/nginx/$domain"
fi

# Delete backend
if [ -n "$BACKEND" ]; then
	prepare_web_backend
	delete_web_backend
fi

#----------------------------------------------------------#
#                       Hestia                             #
#----------------------------------------------------------#

# Rebuilding vhosts
$BIN/h-rebuild-web-domains "$user" "$restart"

# Logging
$BIN/h-log-action "$user" "Info" "Web" "Renamed web domain (Old Domain: $domain, New Domain: $new_domain)."
log_event "$OK" "$EVENT"

exit
